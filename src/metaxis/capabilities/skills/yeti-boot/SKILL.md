---
name: yeti-boot
version: 1.0.0
description: Deterministic PROTOS-4 boot brief for the YKS Ops control plane.
---

# Yeti Boot

Treat `Yeti's live`, `Yeti’s live`, `Yeti live`, `Yetis live`, and
`Yeti's life` as boot triggers for operator Yeti.

Return the YKS Ops live brief from METAXIS control-plane state. Identify the
active surface, root authority, SAFe chain, brain route, storage backend,
GitHub broker posture, classification denial, and next safe action.

This skill is deterministic and read-only. It must not call a model, expose a
credential, authorize a consequential action, claim current external state
that the runtime cannot verify, or convert a loaded plugin into an active one.

GitHub, D1, and canon remain the durable body. NemaShells is presentation
only. No issue may be executable when it is orphaned from the authority chain.
